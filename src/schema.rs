table! {
    tests (id) {
        id -> Int4,
        name -> Varchar,
        color -> Int4,
        document_id -> Varchar,
        size -> Int4,
        comment -> Text,
        user_id -> Varchar,
        user_name -> Varchar,
        created_at -> Timestamp,
    }
}
